# Core dependencies — deployed everywhere (Streamlit Community Cloud, Railway).
# Runs the full agent (price/history/news) + guardrails + web UI.
# For local RAG over PDFs, also install requirements-rag.txt (adds torch etc.).
langchain>=0.3.27
langchain-core>=0.3.79
langchain-anthropic>=0.3.0
langchain-community>=0.3.31
langgraph>=0.3.7
yfinance>=0.2.66
tavily-python>=0.5.0
python-dotenv>=1.0.0
streamlit>=1.40.0
