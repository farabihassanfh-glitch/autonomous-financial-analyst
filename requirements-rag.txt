# Optional RAG extras (local only). Heavy — pulls in torch + a local embedding
# model. Install alongside requirements.txt to enable the Private RAG feature:
#   pip install -r requirements.txt -r requirements-rag.txt
langchain-huggingface>=0.1.2
langchain-chroma>=0.2.0
langchain-text-splitters>=0.3.0
chromadb>=1.3.4
sentence-transformers>=3.0.0
pypdf>=6.0.0
